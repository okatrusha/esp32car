# ESP32-Car YOLO Контролер для переслідування м’яча

Цей проєкт дозволяє керувати ESP32-CAM машинкою, яка стежить за м’ячем у режимі реального часу за допомогою моделі YOLOv8. Комп’ютер отримує зображення з камери машинки через WiFi, виконує детекцію об’єктів і надсилає машинці HTTP-команди руху: `forward`, `left`, `right`, `stop`. Якщо м’яч зникає з кадру, машинка автоматично запускає режим пошуку.

У цьому проєкті реалізовано:
- отримання відеокадру з ESP32-CAM через `/capture`;
- детекцію об’єкта (м’яча) нейромережею YOLO;
- переслідування м’яча з корекцією напрямку;
- алгоритм сканування (sweep search), якщо м’яч втрачено;
- базову логіку стабілізації та обмеження руху;
- автоматичне зупинення при завершенні або помилках.

---

## 📡 Підключення до ESP32-CAM машинки

1. Увімкніть ESP32-CAM машинку.  
2. Вона створить власну WiFi-мережу. Приклад:

SSID: ESP32-CAR
Password: 12345678

3. Під’єднайтесь до цієї мережі з ноутбука.  
4. Перевірте доступність головної сторінки:
`http://192.168.4.1/`

5. Перевірте, чи працює камера:

`http://192.168.4.1/capture`

Якщо в браузері завантажується зображення — підключення успішне.

6. Перевірте можливість керування машинкою через HTTP-запити:

`http://192.168.4.1/action?go=forward`
`http://192.168.4.1/action?go=left`
`http://192.168.4.1/action?go=right`
`http://192.168.4.1/action?go=stop`
`http://192.168.4.1/action?led=on`

Ці команди використовуються всередині основного скрипта.

---

## 🔧 Встановлення та середовище виконання

Щоб запустити скрипт керування, необхідно встановити Python та створити ізольоване середовище `venv`.

### 1. Створення віртуального середовища

```bash
python -m venv venv
```

### 2. Активація середовища
Windows:

```bash
venv\Scripts\activate
```
### 3. Встановлення залежностей
```bash
pip install -r requirements_venv.txt
```
Фреймворк Ultralytics YOLO використовується для детекції об’єктів.

## Запуск програми
Для запуску алгоритму переслідування м’яча:

`python esp32_yolo_chase.py`

Під час запуску:

* вмикається LED на машинці (?);
* скрипт переходить у режим отримання зображення;
* YOLO починає аналіз кадру;
* машинка рухається відповідно до позиції м’яча;
* при втраті цілі активується пошук.

Для завершення роботи натисніть клавішу Q у вікні OpenCV.

# 🤖 Логіка роботи алгоритму
1. Детекція м’яча
YOLO обробляє кожен отриманий кадр. Якщо знайдено об’єкт класу “sports ball” з певною впевненістю, його координати використовуються для навігації.

2. Центрування
Алгоритм визначає горизонтальне зміщення центра м’яча відносно центра кадру й вибирає дію:
* м’яч приблизно по центру → машинка рухається вперед;
* м’яч ліворуч → машинка повертає ліворуч;
* м’яч праворуч → машинка повертає праворуч.
Корекція масштабу залежить від розміру детекції (чим м’яч ближчий — тим менший крок руху).

3. Режим втрати м’яча (search mode)
Якщо YOLO не бачить м’яч протягом кількох кадрів:
* машинка зупиняється;
* активується пошуковий режим.
* Пошук складається з послідовності:
* кілька кроків повороту вліво;
* кілька кроків повороту вправо;
* рух вперед;
* повторення циклу.

Якщо пошук триває занадто довго — режим вимикається, машинка зупиняється.

# 💡 Корисні поради
* Переконайтесь, що ESP32-CAM має достатнє освітлення.
* Якщо відео завантажується повільно — збільшіть timeout або зменште роздільну здатність камери.
* Якщо машинка рухається надто різко — зменшіть STEP_TIME.
* Якщо YOLO знаходить забагато хибних об’єктів — підвищте поріг довіри conf > 0.2.
